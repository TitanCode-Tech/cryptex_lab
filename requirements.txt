# CRYPTEX LAB dependencies.
#
# All of these run locally. No package listed here makes network calls at
# runtime for our use cases (we deliberately omit bitcoinlib and any
# blockchain-RPC client to keep the no-network guarantee easy to audit).
#
# Workflow: install on an online machine, then copy the whole project
# folder (including venv/) to your offline machine.

# UI
streamlit==1.57.0

# BIP39 / BIP32 / BIP44 / BIP49 / BIP84 derivation - pure-python crypto.
bip_utils==2.12.1
mnemonic==0.21

# Forensic / export helpers - pure-python, no network.
pycryptodome==3.23.0
pandas==3.0.3
fpdf2==2.8.7
qrcode==8.2
Pillow==12.2.0

# Test-only. Safe to omit on the offline machine.
pytest==9.0.3

# Live Mode Only - lazy-imported by live_utils.py; never loaded in OFFLINE SAFE mode.
requests==2.31.0

# Cryptography - for license signature verification and build integrity signing
cryptography==48.0.0

# 2FA / TOTP - offline time-based one-time passwords for role elevation
pyotp==2.9.0

# Build-time only: compile security modules to native extensions (developer machine)
# Clients do not need Cython — they receive the pre-compiled .so/.pyd files
cython==3.2.5
setuptools>=68.0

shamir-mnemonic>=0.3.0

# BTCRecover engine — required by the bundled btcrecover/ directory.
# coincurve is a C extension; on Linux/macOS pip builds it from source
# (needs libsecp256k1). On Windows, use the pre-built wheel from offline_wheels/.
coincurve>=20.0.0,<22
protobuf>=5.0.0
